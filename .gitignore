# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*
!.env.example
!server/.env.example

# Playwright / test artifacts (can capture session & trace data)
playwright-report
test-results
playwright/.cache

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ESP32 firmware — config.h holds real Wi-Fi credentials + device tokens.
# Only the config.h.example template is committed.
firmware/agriconnect_esp32_node/config.h

# Local JSON mock database contains real user PII + password hashes.
# It must never be committed. Remove from history if previously tracked.
server/data/users.json
server/data/
