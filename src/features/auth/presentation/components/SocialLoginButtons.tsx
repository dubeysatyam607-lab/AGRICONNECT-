import React from 'react';

/**
 * Social OAuth login options are removed per production directives.
 * Only verified Mobile + OTP and Email + Password authentication are supported.
 */
export const SocialLoginButtons: React.FC = () => {
  return null;
};
